{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": [
                "mkdir -p build && ",
                "cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=1 -S src -B build  && ",
                "cmake --build build"
            ]
        }
    ]
}
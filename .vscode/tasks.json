{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": [
                "mkdir -p build && ",
                "cmake -S src -B build && ",
                "cmake --build build"
            ]
        }
    ]
}